spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  thymeleaf:
    prefix: classpath:/templates/
    cache: false
  datasource:
    url: jdbc:mysql://localhost:3306/exercise?useSSL=false&&serverTimezone=UTC
    username: root
    password: 123456
